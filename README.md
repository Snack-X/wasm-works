# WebAssembly Works

All modules were written by myself in WebAssembly text format(`.wat` files), and compiled to binary format(`.wasm` files).

Highly experimental. Just for fun.

## Modules

* SHA-1 ([source](modules/sha1.wat))
